#include "main.h"
/**
 *mul - multiplies two integers.
 *@a: passed parameter from main.c.
 *@b: passed parameter from main.c.
 *Return: returns the product of two numbers;
 */
int mul(int a, int b)
{
	int prod;

	prod = a * b;

	return (prod);
}
